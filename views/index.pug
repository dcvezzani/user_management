<!DOCTYPE html>
html(lang='en')
  head
    meta(charset="UTF-8")
    title= title
    style
      include ./app.css

  body
    p May Node and Express be with you.
 
    ul(class="quotes")
      each quote, index in quotes
        li(class="quote")
          span= quote.name
          span= quote.quote
      
  <!-- CRUD &#45; CREATE -->
  form(action="/quotes", method="POST")
    input(type="text", placeholder="name", name="name")
    input(type="text", placeholder="quote", name="quote")
    button(type="submit") Submit
