{  
  // Secret key for JWT signing and encryption
  'secret': 'super secret passphrase',
  // Database connection information
  'database': 'mongodb://localhost/user_management_test',
  // Setting port for server
  'port': 3000
}

